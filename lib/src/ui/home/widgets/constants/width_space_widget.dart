import 'package:flutter/material.dart';

class WidthSpaceWidget extends StatelessWidget {
  const WidthSpaceWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 15,
    );
  }
}