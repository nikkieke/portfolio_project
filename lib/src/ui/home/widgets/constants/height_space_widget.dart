import 'package:flutter/material.dart';

class HeightSpaceWidget extends StatelessWidget {
  const HeightSpaceWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15,
    );
  }
}